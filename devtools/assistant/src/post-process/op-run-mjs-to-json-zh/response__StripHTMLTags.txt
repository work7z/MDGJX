根据您的要求，下面是提炼代码中自然文本并转换为JSON格式的步骤：

1. 找出所有显示给用户的自然文本。
2. 将这些文本放入名为`i18n`的对象中，键是文本本身，值设置为123。
3. 根据代码的功能，给出这些自然文本的中文名称。
4. 输出JSON结果。

以下是转换后的结果：

```json
{
  "i18n": {
    "Strip HTML tags": "去除HTML标签",
    "Removes all HTML tags from the input.": "从输入中移除所有HTML标签。",
    "Remove indentation": "去除缩进",
    "Remove excess line breaks": "去除多余换行"
  }
}
```

以下是按照您的示例格式整理后的JSON：

```json
{
  "i18n": {
    "Strip HTML tags": "去除HTML标签",
    "description": "从输入中移除所有HTML标签。",
    "Remove indentation": "去除缩进",
    "Remove excess line breaks": "去除多余换行"
  }
}
```

请注意，我已经将英文描述翻译成中文，并且按照代码的实际功能进行了适当的调整。这些翻译应该能够准确反映代码中每个选项或功能的作用。