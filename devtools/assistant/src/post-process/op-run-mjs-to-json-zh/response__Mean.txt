根据您的要求，以下是提炼代码中自然文本并转换为JSON格式的过程：

1. 找出所有显示给用户的自然文本。
2. 将这些文本放入名为`i18n`的对象中，键是文本，值设置为`123`作为占位符。
3. 根据代码的实际功能，将键对应的值翻译成中文。
4. 输出JSON结果。

下面是转换后的JSON格式：

```json
{
  "i18n": {
    "Computes the mean (average) of a number list. If an item in the string is not a number it is excluded from the list.": "计算数字列表的平均值。如果字符串中的项不是数字，则将其从列表中排除。",
    "e.g. <code>0x0a 8 .5 .5</code> becomes <code>4.75</code>": "例如：<code>0x0a 8 .5 .5</code> 变成 <code>4.75</code>",
    "Mean operation": "平均值操作"
  }
}
```

以下是详细的步骤说明：

- 从代码中提取的自然文本包括类描述、构造函数中的描述和示例。
- 将这些文本作为键值对的键放入`i18n`对象中，值初始设置为`123`，但在这里我已经将它们替换为对应的中文翻译。
- 最后，这个`i18n`对象被嵌入到一个更大的JSON结构中，以便于使用和引用。

请注意，由于代码示例中并没有提供很多自然文本，所以这里只有三个翻译的例子。如果代码中有更多相关的文本，请按照相同的方法进行添加和翻译。