根据您的要求，下面是将代码中显示给用户的自然文本提炼成JSON格式的过程：

1. 找出所有自然文本。
2. 将这些文本放入名为`i18n`的对象中，键是自然文本，值设为`123`。
3. 根据代码的实际功能，翻译这些文本为中文，并替换`123`。
4. 输出JSON结果。

以下是结果：

```json
{
  "i18n": {
    "Extract email addresses": "提取电子邮件地址",
    "Extracts all email addresses from the input.": "从输入中提取所有电子邮件地址",
    "Display total": "显示总数",
    "Sort": "排序",
    "Unique": "唯一"
  }
}
```

解释：
- "Extract email addresses" 是操作的名字，翻译为 "提取电子邮件地址"。
- "Extracts all email addresses from the input." 是操作的描述，翻译为 "从输入中提取所有电子邮件地址"。
- "Display total" 是一个参数的名字，翻译为 "显示总数"。
- "Sort" 是一个参数的名字，翻译为 "排序"。
- "Unique" 是一个参数的名字，翻译为 "唯一"。

这些翻译是根据代码的功能和上下文进行的，应该能准确反映每个键的含义。